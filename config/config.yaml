telegram:
  api_id: ${API_ID}
  api_hash: "${API_HASH}"

bot:
  token: "${BOT_TOKEN}"

notifications:
  user_id: ${USER_ID}

limits:
  notifications_per_minute: 20

health:
  port: 8080

dashboard:
  port: 8081